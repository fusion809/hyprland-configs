#!/usr/bin/env bash
if `which turbo &> /dev/null`; then
	list=$(turbo -yP | grep -v "No.*packages" | grep -v "Update:" | grep -v "^$" | grep -v "json")
	no=$(echo $list | grep -v '^[[:space:]]*$' | wc -l)
fi
if [[ $no -gt 0 ]]; then
	echo "󰚰"
fi
