######################################
# GENERAL
######################################
exec-once = hyprpm enable hy3

# Mod key
$mod = SUPER

# Font (Hyprland doesn't use it directly; waybar/wofi use their own)
# font = pango:Ubuntu 9

######################################
# WINDOW MANAGEMENT
######################################

# Move floating windows with Mod + mouse
#bind = $mod, mouse:272, hy3:movewindow
bind = $mod SHIFT, R, submap, resize
#bind = $mod, mouse:273, resizewindow
submap = resize
    # Resize with arrow keys
    bind = , h, resizeactive, -10 0
    bind = , l, resizeactive, 10 0
    bind = , k, resizeactive, 0 -10
    bind = , j, resizeactive, 0 10

    # Or with vim-style (hjkl) or arrows; choose your preference
    bind = , left,  resizeactive, -10 0
    bind = , right, resizeactive, 10 0
    bind = , up,    resizeactive, 0 -10
    bind = , down,  resizeactive, 0 10

    # Return to normal mode when you press Escape or Super again
    bind = , escape, submap, reset
    bind = $mod, R, submap, reset
submap = reset
# Focus specific apps (Hyprland uses dispatchers + window rules)
# In Hyprland, direct "focus by class" isn’t built-in; you'd use `hyprctl dispatch focuswindow` with regex.
# Example (you might need to adjust regex names):
#bind = HOME, exec, hyprctl dispatch focuswindow "class:.*RuneScape.*"
#bind = F10, exec, hyprctl dispatch focuswindow "class:.*Thunar.*"
#bind = F11, exec, hyprctl dispatch focuswindow "class:.*vlc.*"
#bind = F12, exec, hyprctl dispatch focuswindow "class:.*konsole.*"

# Screenshots / screencast
bind = , Print, exec, /home/fusion809/.local/bin/screenshot

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
# start Rofi
bind = $mod, d, exec, rofi -show drun -show-icons -icon-theme "WhiteSur-dark"

# Launch RuneScape NXT client
bind = $mod, o, exec, /home/fusion809/.local/bin/runescape

# Launch Nautilus
bind = $mod, p, exec, nautilus

# Launch VirtualBox
bind = $mod, v, exec, VirtualBox

# Run wallpaper
bind = $mod, w, exec, /home/fusion809/.local/bin/wallpaper

# Show open windows Rofi interface
bind = $mod Shift, w, exec, rofi -show window

######################################
# WINDOW NAVIGATION
######################################

# Focus (note: Hyprland uses l/r/u/d)
bind = $mod, J, movefocus, l
bind = $mod, K, movefocus, d
bind = $mod, L, movefocus, u
bind = $mod, semicolon, movefocus, r

bind = $mod, left, movefocus, l
bind = $mod, down, movefocus, d
bind = $mod, up, movefocus, u
bind = $mod, right, movefocus, r

# Move windows
bind = $mod SHIFT, J, movewindow, l
bind = $mod SHIFT, K, movewindow, d
bind = $mod SHIFT, L, movewindow, u
bind = $mod SHIFT, semicolon, movewindow, r

bind = $mod SHIFT, left, hy3:movewindow, l
bind = $mod SHIFT, down, hy3:movewindow, d
bind = $mod SHIFT, up, hy3:movewindow, u
bind = $mod SHIFT, right, hy3:movewindow, r

# Fullscreen
bind = $mod, F, fullscreen

# Floating toggle
bind = $mod, SPACE, togglefloating
# Kill window
bind = $mod, Q, hy3:killactive

# Kill hyprland
bind = $mod Shift, e, exec, hyprctl dispatch exit 

######################################
# WORKSPACES
######################################

# Switch workspaces
bind = , F1, workspace, 1
bind = $mod, 2, workspace, 2
bind = , F3, workspace, 3
bind = , F4, workspace, 4
bind = , F5, workspace, 5
bind = , F6, workspace, 6
bind = , F7, workspace, 7
bind = , F8, workspace, 8
bind = , F9, workspace, 9
bind = , F10, workspace, 10

bind = $mod, F1, workspace, 11
bind = $mod, F2, workspace, 12
bind = $mod, F3, workspace, 13
bind = $mod, F4, workspace, 14
bind = $mod, F5, workspace, 15
bind = $mod, F6, workspace, 16
bind = $mod, F7, workspace, 17
bind = $mod, F8, workspace, 18
bind = $mod, F9, workspace, 19
bind = $mod SHIFT, F10, workspace, 20

# Move windows to workspace
bind = $mod SHIFT, 1, movetoworkspacesilent, 1
bind = $mod SHIFT, 2, movetoworkspacesilent, 2
bind = $mod SHIFT, 3, movetoworkspacesilent, 3
bind = $mod SHIFT, 4, movetoworkspacesilent, 4
bind = $mod SHIFT, 5, movetoworkspacesilent, 5
bind = $mod SHIFT, 6, movetoworkspacesilent, 6
bind = $mod SHIFT, 7, movetoworkspacesilent, 7
bind = $mod SHIFT, 8, movetoworkspacesilent, 8
bind = $mod SHIFT, 9, movetoworkspacesilent, 9
bind = $mod SHIFT, 0, movetoworkspacesilent, 10

bind = $mod SHIFT, F1, movetoworkspacesilent, 11
bind = $mod SHIFT, F2, movetoworkspacesilent, 12
bind = $mod SHIFT, F3, movetoworkspacesilent, 13
bind = $mod SHIFT, F4, movetoworkspacesilent, 14
bind = $mod SHIFT, F5, movetoworkspacesilent, 15
bind = $mod SHIFT, F6, movetoworkspacesilent, 16
bind = $mod SHIFT, F7, movetoworkspacesilent, 17
bind = $mod SHIFT, F8, movetoworkspacesilent, 18
bind = $mod SHIFT, F9, movetoworkspacesilent, 19
bind = $mod SHIFT, F10, movetoworkspacesilent, 20

######################################
# TERMINAL
######################################

bind = $mod, tab, exec, alacritty
bind = $mod, RETURN, exec, alacritty
bind = $mod, k, exec, kitty
bind = $mod Shift, tab, exec, focus-terminal

######################################
# SYSTEM CONTROLS
######################################

# Volume/media keys
bind = , XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bind = , XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
bind = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioNext, exec, playerctl next
bind = , XF86AudioPrev, exec, playerctl previous

general {
	gaps_in = 0
	gaps_out = 0
	layout = hy3
}
# Extra keys
bind = , XF86HomePage, exec, nautilus
bind = , XF86Calculator, exec, octave

######################################
# AUTOSTART
######################################
$browser = google-chrome-stable
exec-once = $browser
# exec-once = hexchat
exec-once = waybar

bind = $mod, r, exec, hyprctl reload; pkill waybar; waybar
# Environment variables
#$terminal = kitty
$terminal = alacritty
$fileManager = nautilus
$menu = rofi -show window

# Window rules
exec-once = bash -c "/home/fusion809/GitHub/mine/config/hyprland-configs/install.sh; sleep 2"
exec-once = bash -c "/home/fusion809/.local/bin/wallpaper"
windowrulev2 = opacity 0.8 0.8,class:^Alacritty$
windowrulev2 = opacity 0.6 0.6,class:^kitty$

bind = , F11, exec, ~/.local/bin/focus-chrome
bind = , F12, exec, ~/.local/bin/focus-nautilus
#exec-once = hyprctl setcursor WhiteSur-dark 24
exec-once = /usr/bin/gnome-keyring-daemon --daemonize --login
exec-once = /usr/lib/polkit-1/polkitd --no-debug --log-level=notice
exec-once = /home/fusion809/.local/bin/bluetooth-connect
#env = HYPRCURSOR_THEME,Bibata-Modern-Ice
env = HYPRCURSOR_THEME,"Future-Cyan-Hyprcursor_Theme"
env = HYPRCURSOR_SIZE,24
plugin {
    hy3 {
	tabs {
		border_width = 0
		col.active = rgba(ff55ffff)
		col.active.text = rgba(00000000)
		col.inactive = rgba(333333ff)
		col.inactive.text = rgba(ffffffff)
		col.urgent = rgba(ff0000ff)
		col.urgent.text = rgba(000000ff)
	}

        autotile {
            enable = true
            trigger_width = 800
            trigger_height = 500
        }
    }
}
#bind = $mod, t, hy3:togglefocuslayer
#bind = $mod, s, hy3:makegroup, h
#bind = $mod, z, hy3:makegroup, v
bind = $mod, u, hy3:makegroup, tab
#bind = $mod, a, hy3:changefocus, raise
bind = $mod, a, hy3:expand, expand
